opendpi: 1.0.0
info:
  title: retail
  version: 1.0.0
  description: retail data
connections: {}
ports:
  user_events:
    description: user events table
    connections: []
    schema:
      $ref: schemas/user_events.schema.yaml
  products:
    description: product catalog
    connections: []
    schema:
      $ref: schemas/products.schema.yaml
  stores:
    description: store locations
    connections: []
    schema:
      $ref: schemas/stores.schema.yaml
  orders:
    description: customer orders
    connections: []
    schema:
      $ref: schemas/orders.schema.yaml
  customers:
    description: customer profiles
    connections: []
    schema:
      $ref: schemas/customers.schema.yaml
  order_lines:
    description: order line items
    connections: []
    schema:
      $ref: schemas/order_lines.schema.yaml
  inventory:
    description: store inventory levels
    connections: []
    schema:
      $ref: schemas/inventory.schema.yaml
